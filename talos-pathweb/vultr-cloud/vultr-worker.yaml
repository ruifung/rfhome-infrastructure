machine:
  nodeLabels:
    role: worker
    location: vultr-sg
    provider: vultr
  sysctls:
    user.max_user_namespaces: "11255"
    kernel.domainname: pathweb.cloud.yrf.me
  install:
    disk: /dev/vda
