---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: paperless-ngx-data
  namespace: home-network
  labels:
    home.yrf.me/backup-to-okinawa-s3: "true"
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
  storageClassName: okinawa-hdd-nfs
