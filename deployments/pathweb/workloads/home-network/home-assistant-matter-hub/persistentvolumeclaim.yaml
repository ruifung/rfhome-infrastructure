---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: home-assistant-matter-hub-data
  namespace: home-network
  labels:
    home.yrf.me/backup-to-okinawa-s3: "true"
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 2Gi
  storageClassName: cephfs
