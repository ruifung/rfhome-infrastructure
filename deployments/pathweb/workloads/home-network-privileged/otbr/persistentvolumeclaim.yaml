---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: otbr-data
  namespace: home-network-privileged
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: cephfs
