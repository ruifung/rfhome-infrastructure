---
apiVersion: "k8s.cni.cncf.io/v1"
kind: NetworkAttachmentDefinition
metadata:
  name: rfhome-iot-network
  namespace: home-network
spec:
  config: |-
    {
        "cniVersion": "0.3.0",
        "type": "bridge",
        "bridge": "br0",
        "vlan": 55,
        "ipam": {
            "type": "dhcp"
        }
    }