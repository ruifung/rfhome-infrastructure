---
apiVersion: v1
kind: Service
metadata:
  name: imageswap
  namespace: imageswap-system
  labels:
    apps.kubernetes.io/name: imageswap
    resource: service
spec:
  ports:
  - name: https
    port: 443
    targetPort: 5000
  selector:
    apps.kubernetes.io/name: imageswap
  sessionAffinity: None
  type: ClusterIP